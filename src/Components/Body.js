import MainContainer from "./MainContainer";
import Header from "./Header";

const Body = () => {
  return (
    <div>
      <Header hideTopUp="no-need" />
      <MainContainer />
    </div>
  );
};

export default Body;
